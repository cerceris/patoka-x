export class Consts {
    // Plugins.
    static PLUGIN_BASIC = 'basic';
    static PLUGIN_HEADLESS_BROWSER = 'headless_browser';

    // Destination: controller, client, worker.
    // Outgoing: controller, client.
    // Incoming: worker.
    static DEST_CONTROLLER = 'controller';
    static DEST_CLIENT = 'client';
    static DEST_WORKER = 'worker';
}
